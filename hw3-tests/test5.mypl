# while loops, for loops, and conditionals (if statements)

fun int main()

  # minimal while loop
  while true do
  end

  # basic while loop one expression
  while x != 1 do
    x = x - 1
  end

  # nested while loop
  while x < n do
    while y < n do
      y = y + 1
    end
    x = x + 1
  end

  # TODO: additional while tests as needed


  # TODO: for loops
  for i = 0 to 10 do
    x = y + x + i*10000000000000
  end

  for i = neg 99999 to x do
    x = x * x
  end

  y = 1
  for i = 0 to x do
    y = y * x-i 
  end

  # TODO: if statements
  if x > 0 then
    print(x)
  end

  if x > 0 then
    print(x)
  elseif x < neg 9999 then
    print("WOW!")
  elseif y != x then
    print("Thought so")
  elseif y > x then
    print("No way")
  else
    print("HMMMMMMMMMMM")
  end


  # TODO: nested for, while, if variants
  var parameter:UltimateAnswerCoordinate = nil
  if x > 1 then
    if y > 2 then
      if 4 > 3 then
        if a < b then
         doStuff(a, b, c, d, e, f)
	end
        doMoreStuff(a, b)
      calculateTheUltimateAnswer(parameter, f, h, lmnop)
      end
    for i = 0 to g do
      buildNewEarth()
    end
    end
    while g <= h do
      parameter = parameter*parameter
    end
  end


end

